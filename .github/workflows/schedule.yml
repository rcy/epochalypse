name: CRON

on:
  schedule:
    #- cron: '20 * * * *'
    - cron: '* * * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test
      - run: echo "the job is run $DATE"
